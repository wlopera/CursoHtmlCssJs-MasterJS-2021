// Entrda de datos
const a = Number(prompt("Ingrese un número!"));
const b = Number(prompt("Ingrese un número!"));

// Operacion
// const sum = parseInt(a) + parseInt(b);
const sum = a + b;

//Salida de datos
document.write("La suma es: ", sum);
