// Entrada de datos
const a = Number(prompt("Ingrese un número!"));
const b = Number(prompt("Ingrese un número!"));

// Operaciones
const cociente = a / b;
const residuo = a % b;

//Salida de datos
document.write("<h3>El Residuo es: ", cociente, "</h3>");
document.write("<br><h3>El cociente es: ", residuo, "</h3>");
